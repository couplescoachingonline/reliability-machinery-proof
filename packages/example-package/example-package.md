# Example package

Purpose: provide a harmless deterministic example for testing three-part package conformity.

Input: a JSON object containing two numbers, `left` and `right`.

Output: a JSON object containing their sum.

Acceptance example: `{"left": 2, "right": 3}` produces `{"sum": 5}`.
